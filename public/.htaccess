<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Keep real files/directories accessible.
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # Send all client-side routes to React entry.
  RewriteRule ^ index.html [L]
</IfModule>

